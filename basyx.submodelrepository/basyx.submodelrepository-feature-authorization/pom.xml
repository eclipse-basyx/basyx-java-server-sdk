<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.eclipse.digitaltwin.basyx</groupId>
		<artifactId>basyx.submodelrepository</artifactId>
		<version>${revision}</version>
	</parent>

	<artifactId>basyx.submodelrepository-feature-authorization</artifactId>

	<name>BaSyx Submodelrepository feature-authorization</name>
	<description>BaSyx Submodelrepository feature-authorization</description>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-shade-plugin</artifactId>
				<executions>
					<execution>
						<phase>install</phase>  <!-- When to run the plugin
						(during packaging) -->
						<goals>
							<goal>shade</goal>  <!-- The goal of the plugin -->
						</goals>
						<configuration>
							<relocations>
								<relocation>
									<!-- The original package to relocate -->
									<pattern>basyx.submodelrepository-client</pattern>
									<!-- The new namespace where relocated
									classes will live -->
									<shadedPattern>basyx.submodelrepository-shaded-client</shadedPattern>
								</relocation>
								<relocation>
									<!-- The original package to relocate -->
									<pattern>basyx.submodelrepository-core</pattern>
									<!-- The new namespace where relocated
									classes will live -->
									<shadedPattern>basyx.submodelrepository-shaded-core</shadedPattern>
								</relocation>
								<relocation>
									<!-- The original package to relocate -->
									<pattern>basyx.submodelservice-core</pattern>
									<!-- The new namespace where relocated
									classes will live -->
									<shadedPattern>basyx.submodelservice-shaded-core</shadedPattern>
								</relocation>
								<relocation>
									<!-- The original package to relocate -->
									<pattern>basyx.submodelservice-client</pattern>
									<!-- The new namespace where relocated
									classes will live -->
									<shadedPattern>basyx.submodelservice-shaded-client</shadedPattern>
								</relocation>
							</relocations>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>


	<dependencies>
		<dependency>
			<groupId>org.eclipse.digitaltwin.basyx</groupId>
			<artifactId>basyx.submodelrepository-core</artifactId>
		</dependency>
		<dependency>
			<groupId>org.eclipse.digitaltwin.basyx</groupId>
			<artifactId>basyx.http</artifactId>
			<scope>test</scope>
			<classifier>tests</classifier>
		</dependency>
		<dependency>
			<groupId>org.eclipse.digitaltwin.basyx</groupId>
			<artifactId>basyx.authorization</artifactId>
			<scope>test</scope>
			<classifier>tests</classifier>
		</dependency>
		<dependency>
			<groupId>org.eclipse.digitaltwin.basyx</groupId>
			<artifactId>basyx.http</artifactId>
		</dependency>
		<dependency>
			<groupId>org.eclipse.digitaltwin.basyx</groupId>
			<artifactId>basyx.authorization</artifactId>
		</dependency>
		<dependency>
			<groupId>org.eclipse.digitaltwin.basyx</groupId>
			<artifactId>basyx.submodelrepository-http</artifactId>
			<classifier>tests</classifier>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.eclipse.digitaltwin.basyx</groupId>
			<artifactId>basyx.submodelrepository-http</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.eclipse.digitaltwin.basyx</groupId>
			<artifactId>basyx.submodelrepository-backend-inmemory</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.eclipse.digitaltwin.basyx</groupId>
			<artifactId>basyx.submodelservice-backend-inmemory</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.eclipse.digitaltwin.basyx</groupId>
			<artifactId>basyx.authorization.rules.rbac.backend.inmemory</artifactId>
		</dependency>
		<dependency>
			<groupId>org.eclipse.digitaltwin.basyx</groupId>
			<artifactId>basyx.authorization.rules.rbac.backend.submodel</artifactId>
		</dependency>
		<dependency>
			<groupId>org.apache.httpcomponents.client5</groupId>
			<artifactId>httpclient5</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>commons-io</groupId>
			<artifactId>commons-io</artifactId>
		</dependency>
	</dependencies>
</project>
