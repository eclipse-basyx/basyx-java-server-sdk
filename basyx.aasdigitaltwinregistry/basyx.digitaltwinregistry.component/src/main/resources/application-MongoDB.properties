basyx.backend=MongoDB
basyx.aasdiscoveryservice.mongodb.collectionName=${AUTHENTICATION_DATABASE:db-name}
registry.type=MongoDB
registry.discovery.enabled=true
spring.config.activate.on-profile=MongoDB
spring.mongodb.authentication-database=${AUTHENTICATION_DATABASE:db-name}
spring.mongodb.database=${AUTHENTICATION_DATABASE:db-name}
spring.mongodb.host=${DATABASE_HOST:localhost}
spring.mongodb.port=${DATABASE_PORT:27017}
spring.mongodb.username=${DATABASE_USERNAME:db-username}
spring.mongodb.password=${DATABASE_PASSWORD:db-password}
