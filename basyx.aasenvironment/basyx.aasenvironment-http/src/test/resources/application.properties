server.port=8081
# Do NOT change this port if running in a Docker container.
# Changing this will make the service unreachable from outside the container.
# The port should only be changed when the application is not running in a container
basyx.backend = InMemory

basyx.environment = classpath:fileSerializationTest.aasx